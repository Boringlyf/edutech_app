import 'package:flutter/material.dart';

class TeachersOptions extends StatelessWidget {
  const TeachersOptions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
        /*teacher options should be same as students except for the fact that
      teachers will be able to make assignments and quizzes they will however 
      also first be navigated to subjects Screen which will display all subjects and
      classes the teacher is currently teaching ( the content of subject screen will 
      be displayed based on the erp with which user logged in) */
        );
  }
}
